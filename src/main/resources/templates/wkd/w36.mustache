{{>layout/header}}

<div class="container">
    <div class="sub-title">
        <h4>댐수문정보 일자료</h4>
    </div>

    <div class="textAlignCenter searchBox">

        <label for="damcd">관측소코드(필수) : </label>
        <input id="damcd" type="text" style="margin-left:10px;"/>

        <label for="startdt	">관측기간(시작일): </label>
        <input id="startdt" type="text" style="margin-left:10px;"/>
        ~
        <label for="enddt">관측기간(종료일): </label>
        <input id="enddt" type="text" style="margin-left:10px;"/>

        <input id="btnSearchW36" type="button" class="btn btn-outline-info" style="margin-left:10px;" value="조회"/>
    </div>
    <div class="saveBox">
        <a id="btnSave" href="" class="btn btn-outline-success" style="margin-left:10px;" value="csv로 저장">csv로 저장</a>
    </div>
    <!--목록 출력 영역-->
    <table id="dataTable" class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>관측일시</th>
            <th>저수위(EL.m)</th>
            <th>유입량(㎥/s)</th>
            <th>총 방류량(㎥/s)</th>
            <th>발전방류량(㎥/s)</th>
            <th>여수로방류량(㎥/s)</th>
            <th>기타방류량(㎥/s)</th>
            <th>용수공급량(㎥/s)</th>
            <th>댐유역평균우량(mm)</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#data}}
            <tr>
                <td>{{obsdh}}</td>
                <td>{{rwl}}</td>
                <td>{{iqty}}</td>
                <td>{{tdqty}}</td>
                <td>{{edqty}}</td>
                <td>{{spdqty}}</td>
                <td>{{otltdqty}}</td>
                <td>{{itqty}}</td>
                <td>{{rf}}</td>
            </tr>
        {{/data}}
        {{^data}}
            <tr>
                <td colspan="9">해당 데이터가 없습니다.</td>
            </tr>
        {{/data}}
        </tbody>
    </table>
    <div style="height: 100px;"></div>
</div>


{{>layout/footer}}