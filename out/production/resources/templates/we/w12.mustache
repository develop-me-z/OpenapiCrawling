{{>layout/header}}

<div class="container">
    <div class="sub-title">
        <h4>기상 시자료</h4>
    </div>

    <div class="textAlignCenter searchBox">

        <label for="obscd">관측소코드(필수) : </label>
        <input id="obscd" type="text" style="margin-left:10px;"/>

        <label for="startdt	">관측기간(시작일): </label>
        <input id="startdt" type="text" style="margin-left:10px;"/>
        ~
        <label for="enddt">관측기간(종료일): </label>
        <input id="enddt" type="text" style="margin-left:10px;"/>

        <input id="btnSearchW3" type="button" class="btn btn-outline-info" style="margin-left:10px;" value="조회"/>
    </div>
    <div class="saveBox">
        *관측기간이 6개월 이상인 경우 종료일까지 6개월의 자료가 조회됩니다.*
        <a id="btnSave" href="" class="btn btn-outline-success" style="margin-left:10px;" value="csv로 저장">csv로 저장</a>
    </div>
    <!--목록 출력 영역-->
    <table id="dataTable" class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>관측일시</th>
            <th>기온(℃)</th>
            <th>상대습도(%)</th>
            <th>이슬점온도(℃)</th>
            <th>해면기압(h㎩)</th>
            <th>풍속(m/s)</th>
            <th>풍향(방향)</th>
            <th>일사량(mj/㎡)</th>
            <th>전운량</th>
            <th>적설량(㎝)</th>
            <th>일조량(J)</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#data}}
            <tr>
                <td>{{ymdh}}</td>
                <td>{{ta}}</td>
                <td>{{hm}}</td>
                <td>{{td}}</td>
                <td>{{ps}}</td>
                <td>{{ws}}</td>
                <td>{{wd}}</td>
                <td>{{sihr1}}</td>
                <td>{{catot}}</td>
                <td>{{sdtot}}</td>
                <td>{{sshr1}}</td>
            </tr>
        {{/data}}
        </tbody>
    </table>
    <div style="height: 100px;"></div>
</div>


{{>layout/footer}}